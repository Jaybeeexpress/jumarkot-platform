spring:
  application:
    name: entity-profile-service
  datasource:
    url: ${ENTITY_DB_URL:jdbc:postgresql://localhost:5432/entity_db}
    username: ${DB_USERNAME:jumarkot}
    password: ${DB_PASSWORD:jumarkot_dev}
  flyway:
    enabled: false
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
server:
  port: ${PORT:8088}
  shutdown: graceful
management:
  endpoints:
    web:
      exposure:
        include: health,info
